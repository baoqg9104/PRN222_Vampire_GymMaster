@page  
@model GymMaster_RazorPages.Pages.Dashboard.MemberDashboardModel
@{
    ViewData["Title"] = "Hồ sơ thành viên";
}
 

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h5>Thông tin cá nhân</h5>
                </div>
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="CurrentUser.UserId" />

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.Email" class="control-label"></label>
                            <input asp-for="CurrentUser.Email" class="form-control" readonly />
                            <span asp-validation-for="CurrentUser.Email" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.FirstName" class="control-label">Tên</label>
                            <input asp-for="CurrentUser.FirstName" class="form-control" />
                            <span asp-validation-for="CurrentUser.FirstName" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.LastName" class="control-label">Họ</label>
                            <input asp-for="CurrentUser.LastName" class="form-control" />
                            <span asp-validation-for="CurrentUser.LastName" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.Phone" class="control-label">Điện thoại</label>
                            <input asp-for="CurrentUser.Phone" class="form-control" />
                            <span asp-validation-for="CurrentUser.Phone" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.FitnessGoals" class="control-label">Mục tiêu tập luyện</label>
                            <textarea asp-for="CurrentUser.FitnessGoals" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="CurrentUser.FitnessGoals" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="CurrentUser.MedicalNotes" class="control-label">Ghi chú sức khỏe</label>
                            <textarea asp-for="CurrentUser.MedicalNotes" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="CurrentUser.MedicalNotes" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Cập nhật</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-info text-white">
                    <h5>Gói tập hiện tại</h5>
                </div>
                <div class="card-body">
                    @if (Model.CurrentMembership != null)
                    {
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>Tên gói:</strong> @Model.CurrentMembership.Plan.Name</p>
                                <p><strong>Ngày bắt đầu:</strong> @Model.CurrentMembership.StartDate.ToString("dd/MM/yyyy")</p>
                                <p><strong>Ngày hết hạn:</strong> @Model.CurrentMembership.EndDate.ToString("dd/MM/yyyy")</p>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Mô tả:</strong> @Model.CurrentMembership.Plan.Description</p>
                                <p><strong>Giá:</strong> @Model.CurrentMembership.Plan.Price.ToString("C")</p>
                                <a asp-page="/Membership/Upgrade" class="btn btn-sm btn-outline-primary">Nâng cấp gói</a>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            Bạn chưa đăng ký gói tập nào
                            <a asp-page="/Membership/Register" class="alert-link">Đăng ký ngay</a>
                        </div>
                    }
                </div>
            </div>

            <div class="card">
                <div class="card-header bg-success text-white">
                    <h5>Huấn luyện viên của bạn</h5>
                </div>
                <div class="card-body">
                    @if (Model.ActiveTrainerAssignments != null && Model.ActiveTrainerAssignments.Any())
                    {
                        @foreach (var assignment in Model.ActiveTrainerAssignments)
                        {
                            var today = DateOnly.FromDateTime(DateTime.Today);
                            var isActive = assignment.StartDate <= today && (assignment.EndDate == null || assignment.EndDate >= today);
                            var isFuture = assignment.StartDate > today;

                            <div class="trainer-assignment mb-4 p-3 border rounded @(isActive ? "border-success" : (isFuture ? "border-info" : "border-secondary"))">
                                <div class="d-flex align-items-center">
                                    <div class="flex-shrink-0">
                                        <div class="trainer-avatar @(isActive ? "bg-success" : (isFuture ? "bg-info" : "bg-secondary")) text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 60px; height: 60px;">
                                            <i class="fas fa-user fa-2x"></i>
                                        </div>
                                    </div>
                                    <div class="flex-grow-1 ms-3">
                                        <div class="row">
                                            <div class="col-md-8">
                                                <h5 class="mb-1">@assignment.Trainer.FirstName @assignment.Trainer.LastName</h5>
                                                <p class="mb-1 text-muted">@assignment.Trainer.Email</p>
                                                @if (!string.IsNullOrEmpty(assignment.Trainer.Specialization))
                                                {
                                                    <p class="mb-1"><strong>Chuyên môn:</strong> @assignment.Trainer.Specialization</p>
                                                }
                                                @if (!string.IsNullOrEmpty(assignment.Trainer.Certification))
                                                {
                                                    <p class="mb-1"><strong>Chứng chỉ:</strong> @assignment.Trainer.Certification</p>
                                                }
                                                @if (!string.IsNullOrEmpty(assignment.Goals))
                                                {
                                                    <p class="mb-1"><strong>Mục tiêu:</strong> @assignment.Goals</p>
                                                }
                                            </div>
                                            <div class="col-md-4 text-end">
                                                <div class="assignment-dates">
                                                    <p class="mb-1 text-primary">
                                                        <i class="fas fa-play-circle"></i>
                                                        <strong>Bắt đầu:</strong><br>
                                                        <small>@assignment.StartDate.ToString("dd/MM/yyyy")</small>
                                                    </p>
                                                    @if (assignment.EndDate.HasValue)
                                                    {
                                                        <p class="mb-1 text-warning">
                                                            <i class="fas fa-stop-circle"></i>
                                                            <strong>Kết thúc:</strong><br>
                                                            <small>@assignment.EndDate.Value.ToString("dd/MM/yyyy")</small>
                                                        </p>
                                                        var daysLeft = (assignment.EndDate.Value.ToDateTime(TimeOnly.MinValue) - DateTime.Today).Days;
                                                        @if (daysLeft > 0)
                                                        {
                                                            <span class="badge bg-info">Còn @daysLeft ngày</span>
                                                        }
                                                        else if (daysLeft == 0)
                                                        {
                                                            <span class="badge bg-warning">Hết hạn hôm nay</span>
                                                        }
                                                    }
                                                    else
                                                    {
                                                        <p class="mb-1 text-info">
                                                            <i class="fas fa-infinity"></i>
                                                            <strong>Thời gian:</strong><br>
                                                            <small>Không giới hạn</small>
                                                        </p>
                                                    }

                                                    @* Hiển thị trạng thái *@
                                                    @if (isActive)
                                                    {
                                                        <span class="badge bg-success">Đang hoạt động</span>
                                                    }
                                                    else if (isFuture)
                                                    {
                                                        <span class="badge bg-info">Sắp bắt đầu</span>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                        <div class="mt-2">
                                            <a asp-page="/Trainer/Details" asp-route-id="@assignment.Trainer.UserId"
                                               class="btn btn-sm btn-outline-success me-2">
                                                <i class="fas fa-eye"></i> Xem chi tiết
                                            </a>
                                            <small class="text-muted">
                                                <i class="fas fa-id-card"></i>
                                                Gói: @assignment.Membership?.Plan?.Name
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            Bạn chưa được phân công huấn luyện viên
                            <a asp-page="/TrainerAssignments/Create" asp-route-memberId="@Model.CurrentUser.UserId" class="alert-link">Yêu cầu ngay</a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<form method="post" asp-page="/Account/Logout">
    <button type="submit" class="btn btn-danger">Logout</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}